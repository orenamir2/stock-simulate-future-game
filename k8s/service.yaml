apiVersion: v1
kind: Service
metadata:
  name: possible
  namespace: possible
  labels:
    app.kubernetes.io/name: possible
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: possible
  ports:
    - name: http
      port: 8080
      targetPort: http
