apiVersion: v1
kind: Service
metadata:
  name: possible
  namespace: possible
  labels:
    app.kubernetes.io/name: possible
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: possible
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
